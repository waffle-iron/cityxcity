import { expect } from 'chai';

import { describe, it } from 'mocha';
import { replaceWith } from 'cityxcity/helpers/replace-with';

describe('Unit | Helper | replace with', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = replaceWith(42);
    expect(result).to.be.ok;
  });
});

