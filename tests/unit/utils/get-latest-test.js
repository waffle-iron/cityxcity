import { expect } from 'chai';
import { describe, it } from 'mocha';
import getLatest from 'cityxcity/utils/get-latest';

describe('Unit | Utility | get latest', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = getLatest();
    expect(result).to.be.ok;
  });
});
