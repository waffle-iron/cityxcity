import { expect } from 'chai';
import { describe, it } from 'mocha';
import isTrueFilter from 'cityxcity/utils/is-true-filter';

describe('Unit | Utility | is true filter', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = isTrueFilter();
    expect(result).to.be.ok;
  });
});
