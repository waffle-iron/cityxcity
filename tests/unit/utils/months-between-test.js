import { expect } from 'chai';
import { describe, it } from 'mocha';
import monthsBetween from 'cityxcity/utils/months-between';

describe('Unit | Utility | months between', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = monthsBetween();
    expect(result).to.be.ok;
  });
});
