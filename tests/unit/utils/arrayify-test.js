import { expect } from 'chai';
import { describe, it } from 'mocha';
import arrayify from 'cityxcity/utils/arrayify';

describe('Unit | Utility | arrayify', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = arrayify();
    expect(result).to.be.ok;
  });
});
