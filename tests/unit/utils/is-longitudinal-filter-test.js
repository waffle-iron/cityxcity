import { expect } from 'chai';
import { describe, it } from 'mocha';
import isLongitudinalFilter from 'cityxcity/utils/is-longitudinal-filter';

describe('Unit | Utility | is longitudinal filter', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = isLongitudinalFilter();
    expect(result).to.be.ok;
  });
});
