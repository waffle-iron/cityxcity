import { expect } from 'chai';
import { describe, it } from 'mocha';
import setChoroplethColor from 'cityxcity/utils/set-choropleth-color';

describe('Unit | Utility | set choropleth color', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = setChoroplethColor();
    expect(result).to.be.ok;
  });
});
