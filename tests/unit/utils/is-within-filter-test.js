import { expect } from 'chai';
import { describe, it } from 'mocha';
import isWithinFilter from 'cityxcity/utils/is-within-filter';

describe('Unit | Utility | is within filter', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = isWithinFilter();
    expect(result).to.be.ok;
  });
});
