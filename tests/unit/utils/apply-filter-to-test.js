import { expect } from 'chai';
import { describe, it } from 'mocha';
import applyFilterTo from 'cityxcity/utils/apply-filter-to';

describe('Unit | Utility | apply filter to', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = applyFilterTo();
    expect(result).to.be.ok;
  });
});
