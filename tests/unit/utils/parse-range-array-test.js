import { expect } from 'chai';
import { describe, it } from 'mocha';
import parseRangeArray from 'cityxcity/utils/parse-range-array';

describe('Unit | Utility | parse range array', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = parseRangeArray();
    expect(result).to.be.ok;
  });
});
