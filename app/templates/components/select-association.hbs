{{#power-select-multiple
  options=options
  selected=selected
  placeholder=placeholder
  searchField=searchField
  onchange=(action (mut selected))
  as |resource|
}}
  {{get resource searchField}}
{{/power-select-multiple}}
{{yield}}
