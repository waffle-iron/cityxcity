<nav>
  <div class="logo">
    {{#link-to 'cities' class="cities-link"}}Cities >{{/link-to}}
  </div>
    {{#each model as |city|}}
      <div class="city menu">
        {{city.name}}
      </div>
    {{/each}}
  <div class="menu">
    Login
  </div>
</nav>
<main>
  {{outlet}}
</main>